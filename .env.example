# CloudflareIP-Hosts更新器环境变量配置
# 复制此文件为.env并根据需要修改

# 更新间隔（支持s秒、m分钟、h小时、d天）
UPDATE_INTERVAL=6h

# 目标容器名称，使用逗号分隔多个容器
TARGET_CONTAINERS=container1,container2

# 要更新的域名列表，使用逗号分隔
CF_DOMAINS=tracker.qingwapt.com,tracker.pterclub.com

# 每个域名选择的IP数量
IP_COUNT=1

# 预设首选IP（如果设置，则优先使用此IP而不进行测速）
# 留空则正常使用CloudflareSpeedTest测速
PREFERRED_IP=

# CloudflareSpeedTest额外参数
# 例如：-tl 200 -dn 10
SPEED_TEST_ARGS=

# hosts文件中的标记（用于识别自动更新的部分）
HOSTS_MARKER=# CloudflareIP-HostsUpdater 